ex) 

print bus ObjectId
temp query bus T N R 
ex) limit -> temp query bus T N R limit 숫자 select id;
ex) where 절 -> temp query bus T N R select where "name=='2024-000010' && revision==1" select id;
ex) 복수 검색 -> temp query bus T N R select id attribute.value;

expand bus (T N R) or ObjectId select bus id;

// log 찍게하는 방법
list form select field.setting.value output c:/output/formfieldSetting.txt;

// form에 있는 field.setting.value값을 다 뽑음.
list form select field.setting.value;