1. business Object -> SQL? primary key가 있다
matrix Query Language -> MQL
MQL에서도 PK가 존재
id -> 고유키

내가 소스 짤때 많이 사용. id 갖고 기능 구현, part나 object기준으로 

특정 문서 ID를 찾고 싶을때(코딩 상)

type, name, revision -> TNR -> 각각 id를 가짐.

createObject, findObject든 만들 때 공통으로 들어감
findObject(Context, Type, Name, Rev, '*', owner(소유자)

creator : 생성자

policy 정책) 
- 권한, lifecycle(State) -> 주 목적
- 권한이 구분이 제대로 되게 해야한다(분업)

complete -> release(출시) 수정 권한 X, 데이터 조회

relationship(집합)
- 생각없이 from to하는게 아님.
- cardinality : 부모, 자식이 여러명일 수 있다. 1 or N
- 1:1일 경우 TRN이 하나씩 있다.

channel에다가 command를 여러개 달 수 있음.

jsp로 짜면 디테일로 짤 수 있다.
form을 이제 굳이 jsp를 안 짜도 된다.
form을 만들 때 jsp로 짜지 마라(왠만하면)

vault -> type name revision policy connection Object들이 Vault에 저장이 된다.
국가 별로도 나눈다.

trigger
- type, name, 

action -> 만들고 난 뒤
check -> 만들기 전

help -> 소문자가 필수, 대문자가 Option이다.(ㄹㅇ 중요)