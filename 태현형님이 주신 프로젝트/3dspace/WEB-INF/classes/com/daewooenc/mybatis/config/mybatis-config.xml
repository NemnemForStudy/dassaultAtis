<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<settings>
		<setting name="jdbcTypeForNull" value="NULL" />
	</settings>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
				<dataSource type="POOLED">
					<property name="driver" value="${DEV.driver}"/>
			        <property name="url" value="${DEV.url}"/>
			        <property name="username" value="${DEV.username}"/>
			        <property name="password" value="${DEV.password}"/>
      			</dataSource>
    		</environment>
    		<environment id="production">
      		<transactionManager type="JDBC"/>
      		<dataSource type="POOLED">
		        <property name="driver" value="${PROD.driver}"/>
		        <property name="url" value="${PROD.url}"/>
		        <property name="username" value="${PROD.username}"/>
		        <property name="password" value="${PROD.password}"/>
      		</dataSource>
   		</environment>
  	</environments>
	<mappers>
    	<mapper resource="com/daewooenc/mybatis/mapper/ProjectMapper.xml"/>
    	<mapper resource="com/daewooenc/mybatis/mapper/IF_WorkPackageMapper.xml"/>
    	<mapper resource="com/daewooenc/mybatis/mapper/IF_InterfaceMapper.xml"/>
    	<mapper resource="com/daewooenc/mybatis/mapper/IF_MaterialMapper.xml"/>
  	</mappers>
</configuration>